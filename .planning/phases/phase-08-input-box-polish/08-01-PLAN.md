# Plan: Input Box Polish

## Objective

Apply visual refinements to the CopilotKit chat input box: set corner radius to 8px and add 16px padding below the input so it's not touching the bottom of the window.

## Execution Context

- **Phase**: 8 - Input Box Polish
- **Milestone**: 3 - Chat Panel Visual Enhancements
- **Estimated Scope**: XS (< 30 minutes)
- **Files to Modify**: 1 file

## Context

### Current State
- CopilotKit's `.copilotKitInput` class has `border-radius: 20px` by default
- `.copilotKitInputContainer` has `padding-bottom: 15px` by default
- Project already has CSS overrides in `src/index.css` for CopilotKit components

### Target State
- Input box corner radius: 8px (more subtle, less pill-shaped)
- 16px padding below input box (breathing room from window bottom)

### Discovery Findings
From `/node_modules/@copilotkit/react-ui/dist/index.css`:
- `.copilotKitInput` (line 240-250): The input box wrapper with `border-radius: 20px`
- `.copilotKitInputContainer` (line 251-258): The container with `padding-bottom: 15px`

### Approach
Add CSS overrides to `src/index.css` following the established pattern used for other CopilotKit customizations (feedback controls, scrollbars, user messages).

## Tasks

### Task 1: Add input box corner radius override
**File**: `/Users/neileverette/Desktop/generative-ui-prototype/src/index.css`
**Action**: Add CSS rule to override `.copilotKitInput` border-radius

```css
/* Input box corner radius - 8px for subtle rounded corners */
.copilotKitInput {
  border-radius: 8px !important;
}
```

**Verification**: Inspect input box in browser - should have subtle 8px rounded corners instead of pill shape.

### Task 2: Add bottom padding to input container
**File**: `/Users/neileverette/Desktop/generative-ui-prototype/src/index.css`
**Action**: Add CSS rule to override `.copilotKitInputContainer` padding-bottom

```css
/* Input box bottom padding - 16px breathing room from window edge */
.copilotKitInputContainer {
  padding-bottom: 16px !important;
}
```

**Verification**: Inspect input container in browser - should have visible gap between input box and bottom of chat panel.

### Task 3: Verify build passes
**Command**: `npm run build`
**Verification**: Build completes without errors.

## Verification

1. **Visual Check**:
   - Input box has subtle rounded corners (8px vs previous 20px pill shape)
   - Input box has 16px gap from bottom edge of chat panel

2. **Build Check**: `npm run build` passes without errors

3. **Functionality Check**: Chat input still works (can type, send messages)

## Success Criteria

- [ ] Input box corner radius is 8px
- [ ] Input box has 16px padding below it (not touching window bottom)
- [ ] Build passes with no errors
- [ ] Chat functionality unchanged

## Output

- Modified file: `src/index.css`
- CSS overrides added for `.copilotKitInput` and `.copilotKitInputContainer`
